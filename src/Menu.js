import React from 'react';
import styles from './Main.module.css';

const Menu = () => { 
  return (
    <div className={styles.menu}>
      <p>this is menu</p>
    </div>
  );
};

export default Menu;